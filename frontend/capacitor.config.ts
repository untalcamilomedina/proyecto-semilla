import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.proyectosemilla.app',
  appName: 'Proyecto Semilla',
  webDir: 'out'
};

export default config;
