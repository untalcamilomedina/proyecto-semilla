# ───────────────────────────────────────────────────────────────────
# ACTIVE UPSTREAM — Switch between blue and green
# This file is overwritten by deploy.sh during Blue-Green swap
# ───────────────────────────────────────────────────────────────────

# Current active: BLUE
upstream web_backend {
    server web-blue:8000;
}

upstream frontend_backend {
    server frontend-blue:3000;
}
